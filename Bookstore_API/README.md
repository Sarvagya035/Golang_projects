
# 📚 Golang Book Management API

A simple Book Management API built using **Golang**.  
This project demonstrates basic backend concepts such as routing using Gorilla Mux package, JSON handling, adding, deleting and updating resources using Go.
This is REST based API to list, add, update and delete books. As it for beginners there will be no 3rd party packages, authentication and DB.

---

## ▶️ How to Run the Project
Clone the repository, move into the project directory, build the project, and run the application using the commands below:

### Step 1: Clone the repository
```bash
git clone https://github.com/Sarvagya035/Golang_projects.git
cd Bookstore_API
go build
go run main.go

